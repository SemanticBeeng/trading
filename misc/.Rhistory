ep <- endpoints(tmpenv$ORAN.PA, on="minutes", k=1)
ps <- period.sum(tmpenv$ORAN.PA, ep); ps
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 0,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
#to.minutes5(data(sample_matrix), name=NULL)
ep <- endpoints(tmpenv$ORAN.PA, on="minutes", k=1)
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps
align.time(ps, 5)
align.time(ps, 1)
align.time(ps, 1)
ep <- endpoints(tmpenv$ORAN.PA, on="minutes", k=1)
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps
align.time(ps, 1)
align.time(ps, 5)
ep <- endpoints(tmpenv$ORAN.PA, on="minutes", k=1)
ep <- endpoints(tmpenv$ORAN.PA, on="minutes", k=1); ep
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps
align.time(ps, 1)
align.time(ps, 60)
ts <- align.time(ps, 60)
ep <- endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1); ep
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps <- colnames("time", "price")
tmpenv$ORAN.PA
view(tmpenv$ORAN.PA)
view("tmpenv$ORAN.PA")
View(sample_matrix)
ep <- endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1); ep
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep);
ep <- endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1); ep
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep);
ts <- align.time(ps, 60)
ps <- period.sum(tmpenv$ORAN.PA[, 1], endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1));
ps
to.minutes5(tmpenv$ORAN.PA)
align.time(to.minutes5(tmpenv$ORAN.PA), 60)
tmpenv$ORAN.PA
to.minutes5(tmpenv$ORAN.PA)
to.minutes5(tmpenv$ORAN.PA, colnames("Open", "High", "Low", "Close"))
to.minutes5(tmpenv$ORAN.PA, colnames = c("Open", "High", "Low", "Close"))
to.minutes5(tmpenv$ORAN.PA, colnames = c("Open", "High", "Low", "Close"))
to.minutes5(tmpenv$ORAN.PA)
require(quantmod)
require(FinancialInstrument)
tmpenv <- new.env()
symbol <- "ORAN.PA"
from <- as.Date('2013-07-01')
#to <- from + 516
#tmpenv$ORAN.PA <- xts(1:517, as.Date(from:to))
storageDir <- file.path("/datascience/marketdata/storage")
#setSymbolLookup.FI(storage_method = "rda",
#                   base_dir = file.path("/datascience/marketdata/storage"))
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 0,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
tmpenv$ORAN.PA
head(tmpenv$ORAN.PA)
?gead
?head
head(tmpenv$ORAN.PA, n=50)
head(tmpenv$ORAN.PA, n=50)
head(tmpenv$ORAN.PA, n = 500)
to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
align.time(ohlc, 60)
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
align.time(ohlc, 60)
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
align.time(ohlc, 60)
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 1,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 10,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
ps <- period.sum(tmpenv$ORAN.PA[, 1], endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1));
ps
ts <- align.time(ps, 60)
# To OHLC
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
chartSeries(ohlc, theme="white")
chartSeries(ohlc)
candleChart(ohlc)
chartSeries
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 1,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from + 10,
to = from + 11,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
head(tmpenv$ORAN.PA, n = 500)
# Source: http://www.quantmod.com/examples/data/
ps <- period.sum(tmpenv$ORAN.PA[, 1], endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1));
ps
ts <- align.time(ps, 60)
# To OHLC
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from + 20,
to = from + 21,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
head(tmpenv$ORAN.PA, n = 500)
# Source: http://www.quantmod.com/examples/data/
ps <- period.sum(tmpenv$ORAN.PA[, 1], endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1));
ps
ts <- align.time(ps, 60)
# To OHLC
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes10(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes10(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes60(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes60(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes60(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
ohlc <- to.minutes30(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes1(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
?to.period
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 60)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 10,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
head(tmpenv$ORAN.PA, n = 500)
# Source: http://www.quantmod.com/examples/data/
ps <- period.sum(tmpenv$ORAN.PA[, 1], endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1));
ps
ts <- align.time(ps, 60)
# To OHLC
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 60)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 517,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 60)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 240)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 720)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 1000)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 240)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
install.packages("googleVis')
install.packages("googleVis")
require(quantmod)
require(FinancialInstrument)
tmpenv <- new.env()
symbol <- "AIRP.PA"
from <- as.Date('2000-01-01')
to <- as.Date('2009-01-01')
#tmpenv$ORAN.PA <- xts(1:517, as.Date(from:to))
storageDir <- file.path("/datascience/marketdata/storage")
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = to,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = to,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = FALSE))
symbol <- "AIRP.PA"
from <- as.Date('2000-01-01')
to <- from + 365 * 3
#tmpenv$ORAN.PA <- xts(1:517, as.Date(from:to))
storageDir <- file.path("/datascience/marketdata/storage")
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = to,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
tmpenv <- new.env()
symbol <- "AIRP.PA"
from <- as.Date('2000-01-01')
to <- from + 365 * 1
#tmpenv$ORAN.PA <- xts(1:517, as.Date(from:to))
storageDir <- file.path("/datascience/marketdata/storage")
#setSymbolLookup.FI(storage_method = "rda",
#                   base_dir = file.path("/datascience/marketdata/storage"))
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = to,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
Sys.getenv("R_HISTSIZE")
Sys.setenv(R_HISTSIZE = 1000)
Sys.getenv("R_HISTSIZE")
require(quantmod)
require(FinancialInstrument)
tmpenv <- new.env()
symbol <- "AIRP.PA"
from <- as.Date('2000-01-01')
to <- from + 365 * 1
#tmpenv$ORAN.PA <- xts(1:517, as.Date(from:to))
storageDir <- file.path("/datascience/marketdata/storage")
#setSymbolLookup.FI(storage_method = "rda",
#                   base_dir = file.path("/datascience/marketdata/storage"))
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = to,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
(x <- xts(4:10, Sys.Date()+4:10))
(y <- xts(1:6, Sys.Date()+1:6))
merge(x,y)
merge(x,y, join='inner')
merge(x,y, join='left')
merge(x,y, join='right')
merge.zoo(zoo(x),zoo(y),zoo(x), all=c(TRUE, FALSE, TRUE))
merge(merge(x,x),y,join='left')[,c(1,3,2)]
xi <- xts( , index(x))
xi
merge(y, xi)
load("/datascience/marketdata/storage/AIRP.PA/2000.01.02.AIRP.PA.rda")
View(AIRP.PA)
View(AIRP.PA)
symbol <- "AIRP.PA"
from <- as.Date('2000-01-01')
to <- from + 365
#tmpenv$ORAN.PA <- xts(1:517, as.Date(from:to))
storageDir <- file.path("/datascience/marketdata/storage")
#setSymbolLookup.FI(storage_method = "rda",
#                   base_dir = file.path("/datascience/marketdata/storage"))
tmpenv$ORAN.PA <- na.omit(getSymbols(
tmpenv$allDays <- xts(0, as.Date(from:to))
tmpenv$allDays
tmpenv$allDays
tmpenv$allDays <- xts(0, as.Date(from:to))
tmpenv$allDays <- xts(1:365, as.Date(from:to))
to <- from + 364
tmpenv$allDays <- xts(1:365, as.Date(from:to))
tmpenv$allDays
tmpenv$allDays <- xts(as.Date(from:to))
tmpenv$allDays <- xts(1, as.Date(from:to))
tmpenv$allDays <- xts(1:365, as.Date(from:to))
index(tmpenv$allDays)
as.Date(from:to)
?na.locf
na.locf()
?na.locf
na.aggregate()
?na.aggregate
tmpenv$allDays <- xts(, as.Date(from:to))
tmpenv$allDays <- xts(, as.Date(from:to))
tmpenv$allDays
tmpenv$allDays <- xts( , as.Date(from:to))
tmpenv <- new.env()
symbol <- "AIRP.PA"
from <- as.Date('2000-01-01')
to <- from + 364
# http://artax.karlin.mff.cuni.cz/r-help/library/xts/html/merge.html
tmpenv$allDays <- xts( , as.Date(from:to))
storageDir <- file.path("/datascience/marketdata/storage")
#setSymbolLookup.FI(storage_method = "rda",
#                   base_dir = file.path("/datascience/marketdata/storage"))
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = to,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
merge(tmpenv$ORAN.PA, tmpenv$allDays)
tmp <- merge(tmpenv$ORAN.PA, tmpenv$allDays)
View(tmp)
View(tmp)
View(tmp['2000-04'])
View(tmp['2000-04-10'])
View(tmp['2000-04-19'])
View(tmp['2000-04-21'])
View(tmp['2000-04-20'])
tmp
tmp$allDays[,1]
tmp$allDays[,1] <- -1
tmp$allDays[,1] <- -1:-1
tmp <- merge(tmpenv$ORAN.PA, tmpenv$allDays, fill = -1)
View(tmp['2000-04-21'])
View(tmp['2000-04-24'])
View(tmp['2000-05-1'])
View(tmp[ Price = -1])
View(tmp[ Price == -1])
View(tmp[ tmp$Price == -1])
View(tmp[ index(tmp$Price == -1)])
index(tmp$Price == -1)
tmp$allDays.index(tmp$Price == -1)
tmp$allDays.index(TRUE)
tmp$allDays.index
tmp[2004]
tmp[2000]
tmp[Price > 100]
tmp[tmp$Price > 100]
tmp[tmp$Price < 10]
tmp[tmp$Price == 7.5]
tmp[tmp$Price == 173]
tmp[tmp$Price == -1]
tmp['2000-12']
tmp.index[tmp$Price == -1]
?index
tmp[tmp$Price == -1]
tmp[tmp$Price == -1,]
tmp[tmp$Price == -2,]
tmp[tmp$Price == -1,]
tmp[tmp$Price = -1,]
tmp[tmp$Price - -1 == 0,]
tmp
View(tmp['2000-04-20':'2000-04-24'])
tmp['2000-04-20':'2000-04-24']
tmp['2000-04-20']
tmp['2000-04-20':'2000-04-21']
?loess
?xts::filter
?filter
filter(tmp, tmp$Price == -1)
filter(tmp, filter = tmp$Price == -1)
tmpenv$allDays <- xts(rep(-1, to - from), as.Date(from:to))
tmpenv$allDays <- xts(rep(-1, to - from - 1), as.Date(from:to))
length(as.Date(from:to))
to-from
tmpenv$allDays <- xts(rep(-1, to - from + 1), as.Date(from:to))
tmpenv$allDays
?split.xts
coredate(tmp)
coredata(tmp)
tmp[733]
tmp[732]
ctmp <- coredata(tmp)
ctmp[ctmp$Price == -1]
ctmp[ctmp$Price = -1]
ctmp[ctmp$Price == -1]
ctmp[Price == -1]
ctmp[Price == -1]ctmp
ctmp
class(ctmp)
unclass(ctmp)
head(unclass(ctmp))
ctmp[[Price] == -1]ctmp
ctmp[["Price] == -1]ctmp
ctmp[[,1] == -1]ctmp
ctmp <- as.df(coredata(tmp))
ctmp
head(ctmp)
ctmp <- as.df(coredata(tmp))
""
head(ctmp)
