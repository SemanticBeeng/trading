require(quantmod)
require(FinancialInstrument)
#require(PerformanceAnalytics)
#require(xtsExtra)
#require(rCharts)
#setwd("/datascience/marketdata/storage")
#setDefaults(getSymbols,src='RData')
# get S&P 500 data from FRED (St. Louis Fed)
#ORAN.PA <- na.omit(
#  getSymbols(
#    "2013.07.01.ORAN.PA",
#    src="RData",
#    file.path="2015.01.02.ORAN.PA",
#    dir = "ORAN.PA",
#    extension = "RData",
#    from = "2015-01-01",
#    auto.assign = FALSE,
#    verbose = TRUE
#  )
#)
tmpenv <- new.env()
symbol <- "ORAN.PA"
from <- as.Date('2013-07-01')
to <- from + 516
tmpenv$ORAN.PA <- xts(1:517, as.Date(from:to))
storageDir <- file.path("/datascience/marketdata/storage")
getSymbols(
symbol,
from=from,
to=from+5,
src="FI",
env = tmpenv,
dir=storageDir,
etension="RData",
auto.assign = FALSE,
verbose = TRUE)
tmpenv$ORAN.PA
ORAN.PA
tmpenv$ORAN.PA
tmpenv$ORAN.PA <- getSymbols(
symbol,
from=from,
to=from+5,
src="FI",
env = tmpenv,
dir=storageDir,
etension="RData",
auto.assign = FALSE,
verbose = TRUE)
tmpenv$ORAN.PA
require(quantmod)
require(FinancialInstrument)
#require(PerformanceAnalytics)
#require(xtsExtra)
#require(rCharts)
#setwd("/datascience/marketdata/storage")
#setDefaults(getSymbols,src='RData')
# get S&P 500 data from FRED (St. Louis Fed)
#ORAN.PA <- na.omit(
#  getSymbols(
#    "2013.07.01.ORAN.PA",
#    src="RData",
tmpenv <- new.env()
symbol <- "ORAN.PA"
from <- as.Date('2013-07-01')
to <- from + 516
#tmpenv$ORAN.PA <- xts(1:517, as.Date(from:to))
storageDir <- file.path("/datascience/marketdata/storage")
tmpenv$ORAN.PA <- getSymbols(
symbol,
from=from,
to=from+5,
src="FI",
env = tmpenv,
dir=storageDir,
etension="RData",
auto.assign = FALSE,
verbose = TRUE)
tmpenv$ORAN.PA
setSymbolLookup.FI(storage_method = "RData")
tmpenv$ORAN.PA <- getSymbols(
symbol,
from=from,
to=from+5,
src="FI",
env = tmpenv,
dir=storageDir,
etension="RData",
auto.assign = FALSE,
verbose = TRUE)
setSymbolLookup.FI(storage_method = "rda",
storageDir = file.path("/datascience/marketdata/storage"))
setSymbolLookup.FI(storage_method = "rda",
base_dir = file.path("/datascience/marketdata/storage"))
tmpenv$ORAN.PA <- getSymbols(
symbol,
from=from,
to=from+5,
src="FI",
env = tmpenv,
auto.assign = FALSE,
verbose = TRUE)
tmpenv$ORAN.PA <- getSymbols(
symbol,
from=from,
to=from+5,
src="FI",
env = tmpenv,
dir=storageDir,
etension="RData",
auto.assign = FALSE,
verbose = TRUE)
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
Bid(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
tmpenv$ORAN.PA['2013']
tmpenv$ORAN.PA['2013-07-01']
tmpenv$ORAN.PA['2013-07-01 09']
periodicity(tmpenv$ORAN.PA)
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
to.minutes5(tmpenv$ORAN.PA, "Price")
tmpenv$ORAN.PA <- getSymbols(
symbol,
from = from,
to = from + 1,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE)
tmpenv$ORAN.PA <- getSymbols(
symbol,
from = from,
to = from + 0,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE)
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
tmpenv$ORAN.PA['2013-07-01 09']
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
to.minutes5(tmpenv$ORAN.PA, "Price")
?to.minutes5
to.minutes5(tmpenv$ORAN.PA, name=NULL)
to.minutes5(tmpenv$ORAN.PA, name=NULL, OHLC = TRUE)
to.minutes5(tmpenv$ORAN.PA, name=NULL, OHLC = TRUE)
to.minutes5(tmpenv$ORAN.PA, name=NULL)
data(sample_matrix)
View(sample_matrix)
View(sample_matrix)
endpoints(tmpenv$ORAN.PA, on="minutes")
?endpoints
endpoints(tmpenv$ORAN.PA, on="minutes", k=5)
endpoints(tmpenv$ORAN.PA, on="minutes", k=1)
?period.sum
ep <- endpoints(tmpenv$ORAN.PA, on="minutes", k=1)
ps <- period.sum(tmpenv$ORAN.PA, ep); ps
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 0,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
#to.minutes5(data(sample_matrix), name=NULL)
ep <- endpoints(tmpenv$ORAN.PA, on="minutes", k=1)
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps
align.time(ps, 5)
align.time(ps, 1)
align.time(ps, 1)
ep <- endpoints(tmpenv$ORAN.PA, on="minutes", k=1)
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps
align.time(ps, 1)
align.time(ps, 5)
ep <- endpoints(tmpenv$ORAN.PA, on="minutes", k=1)
ep <- endpoints(tmpenv$ORAN.PA, on="minutes", k=1); ep
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps
align.time(ps, 1)
align.time(ps, 60)
ts <- align.time(ps, 60)
ep <- endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1); ep
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep); ps <- colnames("time", "price")
tmpenv$ORAN.PA
view(tmpenv$ORAN.PA)
view("tmpenv$ORAN.PA")
View(sample_matrix)
ep <- endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1); ep
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep);
ep <- endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1); ep
ps <- period.sum(tmpenv$ORAN.PA[, 1], ep);
ts <- align.time(ps, 60)
ps <- period.sum(tmpenv$ORAN.PA[, 1], endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1));
ps
to.minutes5(tmpenv$ORAN.PA)
align.time(to.minutes5(tmpenv$ORAN.PA), 60)
tmpenv$ORAN.PA
to.minutes5(tmpenv$ORAN.PA)
to.minutes5(tmpenv$ORAN.PA, colnames("Open", "High", "Low", "Close"))
to.minutes5(tmpenv$ORAN.PA, colnames = c("Open", "High", "Low", "Close"))
to.minutes5(tmpenv$ORAN.PA, colnames = c("Open", "High", "Low", "Close"))
to.minutes5(tmpenv$ORAN.PA)
require(quantmod)
require(FinancialInstrument)
tmpenv <- new.env()
symbol <- "ORAN.PA"
from <- as.Date('2013-07-01')
#to <- from + 516
#tmpenv$ORAN.PA <- xts(1:517, as.Date(from:to))
storageDir <- file.path("/datascience/marketdata/storage")
#setSymbolLookup.FI(storage_method = "rda",
#                   base_dir = file.path("/datascience/marketdata/storage"))
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 0,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
tmpenv$ORAN.PA
head(tmpenv$ORAN.PA)
?gead
?head
head(tmpenv$ORAN.PA, n=50)
head(tmpenv$ORAN.PA, n=50)
head(tmpenv$ORAN.PA, n = 500)
to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
align.time(ohlc, 60)
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
align.time(ohlc, 60)
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
align.time(ohlc, 60)
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 1,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 10,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
ps <- period.sum(tmpenv$ORAN.PA[, 1], endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1));
ps
ts <- align.time(ps, 60)
# To OHLC
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
chartSeries(ohlc, theme="white")
chartSeries(ohlc)
candleChart(ohlc)
chartSeries
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 1,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from + 10,
to = from + 11,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
head(tmpenv$ORAN.PA, n = 500)
# Source: http://www.quantmod.com/examples/data/
ps <- period.sum(tmpenv$ORAN.PA[, 1], endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1));
ps
ts <- align.time(ps, 60)
# To OHLC
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from + 20,
to = from + 21,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
head(tmpenv$ORAN.PA, n = 500)
# Source: http://www.quantmod.com/examples/data/
ps <- period.sum(tmpenv$ORAN.PA[, 1], endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1));
ps
ts <- align.time(ps, 60)
# To OHLC
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes10(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes10(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes60(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes60(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes60(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
ohlc <- to.minutes30(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes1(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.minutes5(tmpenv$ORAN.PA[,1])
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
?to.period
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 60)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 10,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
has.Vo(tmpenv$ORAN.PA)
has.Ask(tmpenv$ORAN.PA)
has.Bid(tmpenv$ORAN.PA)
has.HLC(tmpenv$ORAN.PA)
#tmpenv$ORAN.PA['2013-07-01 09']
p <- periodicity(tmpenv$ORAN.PA)
unclass(p)
head(tmpenv$ORAN.PA, n = 500)
# Source: http://www.quantmod.com/examples/data/
ps <- period.sum(tmpenv$ORAN.PA[, 1], endpoints(tmpenv$ORAN.PA, on = "minutes", k = 1));
ps
ts <- align.time(ps, 60)
# To OHLC
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 60)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
tmpenv$ORAN.PA <- na.omit(getSymbols(
symbol,
from = from,
to = from + 517,
src = "FI",
env = tmpenv,
dir = storageDir,
etension = "RData",
auto.assign = FALSE,
verbose = TRUE))
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 60)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 240)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 720)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 1000)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
ohlc <- to.period(tmpenv$ORAN.PA[,1], period = "minutes", k = 240)
ohlc <- align.time(ohlc, 60)
chartSeries(ohlc)
